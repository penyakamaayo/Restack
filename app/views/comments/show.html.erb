<p id="notice"><%= notice %></p>

<p>
  <strong>Body:</strong>
  <%= @comment.body %>
</p>

<p>
  <strong>User:</strong>
  <%= @comment.user_id %>
</p>

<p>
  <strong>Post:</strong>
  <%= @comment.post_id %>
</p>

	<%= render 'replies/index', replies: Reply.all %>
	<%= render 'replies/form', reply: Reply.new , comment_id: @comment.id %>

<%= link_to 'Edit', edit_comment_path(@comment) %> |
<%= link_to 'Back', post_path() %>
