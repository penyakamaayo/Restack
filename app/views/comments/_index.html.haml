%p#notice= notice
%br/
%h4 Comments
%table
  %thead
    %tr
      %th Body
      %th User
      %th Post
      %th{:colspan => "3"}
  %tbody
    - comments.each do |comment|
      %tr
        %td= comment.body
        %td= User.find(comment.user_id).email
        %td= comment.post_id
        %td= link_to 'Reply to Comment', comment
%br/
